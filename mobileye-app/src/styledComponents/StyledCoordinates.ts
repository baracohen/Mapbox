import styled from '@emotion/styled';
import { Grid } from '@mui/material';

export const Coordinates = styled(Grid)`
    background-color: rgb(35 55 75 / 90%);
    color: #fff;
    padding: 12px 16px;
    font-family: monospace;
    z-index: 1;
    position: absolute;
    top: 0;
    left: 0;
    margin: 12px;
    border-radius: 4px;
`;